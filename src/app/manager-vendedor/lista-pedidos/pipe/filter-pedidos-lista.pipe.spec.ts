import { FilterPedidosListaPipe } from './filter-pedidos-lista.pipe';

describe('FilterPedidosListaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPedidosListaPipe();
    expect(pipe).toBeTruthy();
  });
});
