import { StateBadgeDirective } from './state-badge.directive';

describe('StateBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new StateBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
