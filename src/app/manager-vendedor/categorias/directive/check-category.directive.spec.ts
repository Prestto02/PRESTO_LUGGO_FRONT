import { CheckCategoryDirective } from './check-category.directive';

describe('CheckCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
