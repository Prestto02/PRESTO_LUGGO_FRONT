import { FilterCategorias } from './FilterCategorias.pipe';

describe('FilterCategorias', () => {
  it('create an instance', () => {
    const pipe = new FilterCategorias();
    expect(pipe).toBeTruthy();
  });
});
