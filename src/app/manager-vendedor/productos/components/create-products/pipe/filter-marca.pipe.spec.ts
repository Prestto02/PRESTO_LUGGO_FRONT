import { FilterMarcaPipe } from './filter-marca.pipe';

describe('FilterMarcaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMarcaPipe();
    expect(pipe).toBeTruthy();
  });
});
