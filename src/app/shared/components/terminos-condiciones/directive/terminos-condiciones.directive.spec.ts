import { TerminosCondicionesDirective } from './terminos-condiciones.directive';

describe('TerminosCondicionesDirective', () => {
  it('should create an instance', () => {
    const directive = new TerminosCondicionesDirective();
    expect(directive).toBeTruthy();
  });
});
