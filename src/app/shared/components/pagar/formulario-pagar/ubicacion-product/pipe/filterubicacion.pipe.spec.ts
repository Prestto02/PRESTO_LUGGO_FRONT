import { FilterubicacionPipe } from './filterubicacion.pipe';

describe('FilterubicacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterubicacionPipe();
    expect(pipe).toBeTruthy();
  });
});
