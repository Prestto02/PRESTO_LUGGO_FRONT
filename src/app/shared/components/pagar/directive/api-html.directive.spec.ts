import { ApiHtmlDirective } from './api-html.directive';

describe('ApiHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new ApiHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
