import { SearchBuyPipe } from './search-buy.pipe';

describe('SearchBuyPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBuyPipe();
    expect(pipe).toBeTruthy();
  });
});
