import { ColorBadgeDirective } from './color-badge.directive';

describe('ColorBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
