import { IdentifyUsersDirective } from './identify-users.directive';

describe('IdentifyUsersDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentifyUsersDirective();
    expect(directive).toBeTruthy();
  });
});
