import { PermisesUsersDirective } from './permises-users.directive';

describe('PermisesUsersDirective', () => {
  it('should create an instance', () => {
    const directive = new PermisesUsersDirective();
    expect(directive).toBeTruthy();
  });
});
