import { SearchPoliticsPipe } from './search-politics.pipe';

describe('SearchPoliticsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPoliticsPipe();
    expect(pipe).toBeTruthy();
  });
});
